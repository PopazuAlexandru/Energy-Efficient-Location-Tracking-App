name: energy_efficient_location_tracking
description: A research project on energy-efficient location tracking.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^11.0.0
  geofencing_2: ^2.1.0
  battery_plus: ^6.0.0
  permission_handler: ^11.3.0
  flutter_foreground_task: ^6.1.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
